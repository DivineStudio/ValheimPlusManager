<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

  <targets>
    <target name="debugLog" xsi:type="File" fileName="debugLog.txt" />
    <target name="releaseLog" xsi:type="File" fileName="Log.txt" />
  </targets>

  <rules>
    <!-- The debugLog rule is disabled by default but can be enabled by setting: enabled="true" -->
    <logger name="*" enabled="false" minlevel="Fatal" maxlevel="Error" writeTo="debugLog" />
    <logger name="*" minlevel="Warn" maxlevel="Trace" writeTo="releaseLog" />
  </rules>
</nlog>
